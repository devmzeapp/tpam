{
  "buildCommand": "rm -rf node_modules/.prisma node_modules/@prisma/client && prisma generate --no-cache && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
